# CI Workflows

For the development coding agent only: edit GitHub Actions here. Keep credentialed LLM runs explicitly controlled and separate from untrusted pull requests. Do not reintroduce unit-test infrastructure for agent behavior.
