# Development Tooling

> **Role:** development-agent instructions for the repository's development tooling scripts. Not product behavior.

For the development coding agent only: maintain validation, packaging, environment setup, and credential-injection helpers. Exclude every AGENTS.md from release payloads. Never print credentials or treat JSON syntax validation as proof of runtime compatibility. Keep generated artifacts outside source control.
