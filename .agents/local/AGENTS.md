# 本机开发资料

> **Role:** development-agent instructions for device-local development records. Not product behavior.

本目录仅存放本机开发资料，不属于产品定义、runtime 或发布载荷。
维护本文件作为受版本控制的 scoped 指令；机器资料不得提交，包括被忽略的 `DevelopmentMachine.md`，不得强制添加。
需要本机环境事实时，检查 `.agents/local/DevelopmentMachine.md` 是否存在，并按需通过显式路径读取；Git ignore 不代表禁止读取。
文件不存在时，不得假定环境事实。不得记录凭据值，也不得将本目录复制到产品发布载荷。
