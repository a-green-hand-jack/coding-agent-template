# Fixture Runtime

> **Role:** development-agent instructions for the paper-agent fixture's runtime tree. Not product behavior.

Development-only fixture runtime tree used to test the diagram generator. It is
never installed and never released.
