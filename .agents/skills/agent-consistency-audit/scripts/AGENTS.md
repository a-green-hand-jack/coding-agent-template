# Agent Consistency Audit Scripts

> **Role:** development-agent instructions for the agent-consistency-audit helper scripts. Not product behavior.

The audit helper is deterministic, dependency-free, and read-only. Keep it
free of credentials and do not turn it into a product runtime tool.
