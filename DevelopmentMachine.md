# Development Machine

> Secret-free profile of this development machine: versions, documented
> wiring, and evidence status only — never credentials. Regenerate with:
>
>     bash .agents/skills/development-machine-profile/scripts/probe-host.sh \
>       | python3 .agents/skills/development-machine-profile/scripts/render-profile.py \
>           --facts .agents/knowledge/development-machine-facts.md

## Scope

- Covers this machine only; generated by the `development-machine-profile` skill.
- Records facts that affect developing the product Agent: host resources,
  toolchain, agent backends, the Harbor interface surface, and available
  external services.
- No credentials: environment-variable **names**, endpoints, and file paths
  are metadata; secrets are supplied by the operator at run time via
  `${VAR}` expansion and are never printed.
- Probe generated: 2026-09-08T03:17:09

## Host identity

| Property | Value |
|---|---|
| Hostname | user-PC |
| OS | Ubuntu 24.04.4 LTS |
| Kernel | 7.0.0-28-generic |
| Architecture | x86_64 |
| CPU | Intel(R) Xeon(R) Gold 6230 CPU @ 2.10GHz |
| Cores | 40 |
| Memory (total) | 251.5 GiB (263743512 kB) |
| GPU | Quadro P2000, 5120 MiB |

## Storage

| Filesystem | Size | Avail | Use% | Mount |
|---|---|---|---|---|
| /dev/nvme0n1p2 | 879G | 203G | 76% | / |
| efivarfs | 512K | 446K | 13% | /sys/firmware/efi/efivars |
| /dev/nvme0n1p1 | 1.1G | 1.1G | 1% | /boot/efi |
| /dev/sda1 | 3.7T | 2.2T | 41% | /media/user/Files |

## Runtime environment

| Property | Value |
|---|---|
| Shell | /home/linuxbrew/.linuxbrew/bin/fish |
| Package managers | apt, brew, uv, pip3, pip, npm, go |
- `go`: go version go1.26.5 linux/amd64
- `node`: v26.5.0
- `npm`: 11.17.0
- `python3`: Python 3.12.3
- `uv`: uv 0.11.23 (x86_64-unknown-linux-gnu)

## Toolchain versions

| Tool | Path | Version |
|---|---|---|
| git | /usr/bin/git | git version 2.43.0 |
| docker | /usr/bin/docker | Docker version 29.7.2, build a7dcaa6 |
| ssh | /usr/bin/ssh | OpenSSH_9.6p1 Ubuntu-3ubuntu13.19, OpenSSL 3.0.13 30 Jan 2024 |
| make | /usr/bin/make | GNU Make 4.3 |
| gcc | /usr/bin/gcc | gcc (Ubuntu 13.3.0-6ubuntu2~24.04.1) 13.3.0 |
| g++ | /usr/bin/g++ | g++ (Ubuntu 13.3.0-6ubuntu2~24.04.1) 13.3.0 |
| curl | /home/linuxbrew/.linuxbrew/bin/curl | curl 8.21.0 (x86_64-pc-linux-gnu) libcurl/8.21.0 OpenSSL/3.6.3 zlib/1.3.1.zlib-ng brotli/1.2.0 zstd/1.5.7 libidn2/2.3.8 libpsl/0.22.0 libssh2/1.11.1 nghttp2/1.69.0 ngtcp2/1.24.0 nghttp3/1.17.0 mit-krb |
| wget | /usr/bin/wget | GNU Wget 1.21.4 built on linux-gnu. |
| jq | /home/linuxbrew/.linuxbrew/bin/jq | jq-1.8.2 |
| rsync | /usr/bin/rsync | rsync  version 3.2.7  protocol version 31 |
| tar | /usr/bin/tar | tar (GNU tar) 1.35 |
| unzip | /usr/bin/unzip | UnZip 6.00 of 20 April 2009, by Debian. Original by Info-ZIP. |
| zip | /usr/bin/zip | Copyright (c) 1990-2008 Info-ZIP - Type 'zip "-L"' for software license. |
| tmux | /home/linuxbrew/.linuxbrew/bin/tmux | tmux 3.7b |
| tailscale | /usr/bin/tailscale | 1.102.2 |
| python3 | /home/user/.local/lib/orca-system-python-bin/python3 | Python 3.12.3 |
| pip3 | /home/user/.local/bin/pip3 | pip 26.2.1 from /home/user/.local/lib/python3.12/site-packages/pip (python 3.12) |
| node | /home/linuxbrew/.linuxbrew/bin/node | v26.5.0 |
| npm | /home/linuxbrew/.linuxbrew/bin/npm | 11.17.0 |
| go | /home/linuxbrew/.linuxbrew/bin/go | go version go1.26.5 linux/amd64 |
| uv | /home/user/.local/bin/uv | uv 0.11.23 (x86_64-unknown-linux-gnu) |
| hf | /home/user/.local/bin/hf | 1.30.0 |
| orca | /home/user/.local/bin/orca |  |
| codex | /home/user/.local/bin/codex | codex-cli 0.153.4 |
| opencode | /home/user/.opencode/bin/opencode | 1.18.29 |
| claude | /home/user/.local/bin/claude | 2.1.263 (Claude Code) |
| pi | /home/linuxbrew/.linuxbrew/bin/pi | 0.85.1 |
| harbor | /home/user/.local/bin/harbor | 0.20.0 |
| lark-cli | /home/linuxbrew/.linuxbrew/bin/lark-cli | lark-cli version 1.0.93 |

## Agent backends & task executors

| Backend | Version | Role |
|---|---|---|
| codex | codex-cli 0.153.4 | OpenAI-family coding agent backend |
| opencode | 1.18.29 | OpenCode coding agent backend |
| claude | 2.1.263 (Claude Code) | Anthropic-family coding agent backend |
| pi | 0.85.1 | pi coding agent backend |
| orca |  | Orca orchestration CLI |

- `harbor` 0.20.0 — external agent/task execution layer (`/home/user/.local/bin/harbor`)

### Supported agents (from `harbor run --help`, runtime observation)

```
╭─ Agent ──────────────────────────────────────────────────────────────────────╮
│ --agent               -a      [aider|antigravity-cli  Agent to run, or a     │
│                               |claude-code|cline-cli  custom agent import    │
│                               |codex|computer-1|copi  path                   │
│                               lot-cli|cursor-cli|dee  (module.path:ClassNam… │
│                               rflow|devin|dspy-rlm|e  Also accepts an ACP    │
│                               ve|gemini-cli|goose|gr  registry shorthand     │
│                               ok-build|hermes|kimi-c  (e.g.                  │
│                               li|langgraph|mimo|mini  acp:opencode@1.3.9).   │
│                               -swe-agent|nemo-agent|                         │
│                               nop|openclaw|opencode|                         │
│                               openhands|openhands-sd                         │
│                               k|oracle|pi|qwen-coder                         │
│                               |rovodev-cli|swe-agent                         │
│                               |terminus|terminus-1|t                         │
│                               erminus-2|trae-agent|v                         │
│                               ibe|acp:<agent>]                               │
│ --model               -m      <str>                   Model name for the     │
│                                                       agent (can be used     │
│                                                       multiple times)        │
│ --ak,--agent-kwarg            <str>                   Additional agent kwarg │
│                                                       in the format          │
│                                                       'key=value'. You can   │
│                                                       view available kwargs  │
│                                                       by looking at the      │
│                                                       agent's `__init__`     │
│                                                       method. Can be set     │
│                                                       multiple times to set  │
│                                                       multiple kwargs.       │
│ --allow-agent-host            <str>                   Run-specific hostname  │
│                                                       or IP address/CIDR     │
│                                                       merged into the agent  │
│                                                       phase allowlist during │
│                                                       agent.run() only. Can  │
│                                                       be used multiple       │
│                                                       times.                 │
│ --resume-trajectory                                   For multi-step tasks,  │
│                                                       resume the agent's     │
│                                                       native session from    │
│                                                       the previous step      │
│                                                       instead of starting a  │
│                                                       fresh conversation on  │
│                                                       each step. Requires an │
│                                                       agent with native      │
│                                                       resume support.        │
│                                                       Per-step sessions:     │
│                                                       (fresh, resume,        │
│                                                       resume, ...) instead   │
│                                                       of the default (fresh, │
│                                                       fresh, fresh, ...).    │
│ --load-trajectory             <str>                   Path to a trajectory   │
│                                                       (ATIF) to load as the  │
│                                                       agent's session before │
│                                                       the first step. Not    │
│                                                       implemented yet;       │
│                                                       reserved interface.    │
│                                                       Per-step sessions:     │
│                                                       (load, fresh, fresh,   │
│                                                       ...); combined with    │
│                                                       --resume-trajectory:   │
│                                                       (load, resume, resume, │
│                                                       ...).                  │
│ --ae,--agen
```

## External services

| Service | Path | Version | Extra |
|---|---|---|---|
| lark-cli (Feishu/Lark) | /home/linuxbrew/.linuxbrew/bin/lark-cli | lark-cli version 1.0.93 |  |
| hf (Hugging Face) | /home/user/.local/bin/hf | 1.30.0 |  |
| orca (Orca CLI) | /home/user/.local/bin/orca |  |  |
| docker | /usr/bin/docker | Docker version 29.7.2, build a7dcaa6 | daemon: running |

### External CLI command surfaces

- `hf`: `auth`, `buckets`, `cache`, `collections`, `cp`, `datasets`, `discussions`, `download`, `endpoints`, `extensions`, `jobs`, `models`, `papers`, `repos`, `sandbox`, `skills`, `spaces`, `sync`, `upload`, `upload-large-folder`, `webhooks`, `env`, `update`, `version`
- `docker`: `run`, `exec`, `ps`, `build`, `bake`, `pull`, `push`, `images`, `login`, `logout`, `search`, `version`, `info`, `builder`, `container`, `context`, `image`, `manifest`, `network`, `plugin`, `system`, `volume`, `swarm`, `attach`, `commit`, `cp`, `create`, `diff`, `events`, `export`, `history`, `import`, `inspect`, `kill`, `load`, `logs`, `pause`, `port`, `rename`, `restart`, `rm`, `rmi`, `save`, `start`, `stats`, `stop`, `tag`, `top`, `unpause`, `update`, `wait`

`lark-cli` quickstart (from `--help`):

```
lark-cli — Lark/Feishu CLI tool.

AGENT QUICKSTART (driving this as an agent? start here):
    Browse commands:  lark-cli <domain> --help            # +shortcuts (preferred) and raw API resources
    Inspect a call:   lark-cli schema <service>.<resource>.<method>   # params, types, scopes, examples
    Prefer a +shortcut over the raw API resource when one matches the task.
    Risk: each command's --help shows read | write | high-risk-write;
          high-risk-write needs --yes, only after the user confirms.
    On any API call: --jq <expr> filters JSON output, --dry-run previews the request (runs nothing).

EXAMPLES (one per command style, in order of preference):
    lark-cli calendar +agenda                                       # +shortcut — a high-level task, prefer these
    lark-cli mail user_mailbox.messages list --user-mailbox-id me   # typed command for one API method
    lark-cli schema mail.user_mailbox.messages.list                 # inspect a method's params before calling
    lark-cli api GET /open-apis/calendar/v4/calendars               # raw escape hatch — any endpoint by HTTP path

Usage:
  lark-cli <command> [subcommand] [method] [flags]
  lark-cli api <method> <path> [--params <json>] [--data <json>]
  lark-cli schema <service.resource.method>

Lark domains:
  application Open Platform app self-management: slash commands for the currently bound app
  approval    Approval instance, and task management
  apps        Develop, deploy HTML, web pages and applications
  attendance  Attendance check-in result query
  base        Table, field, record, view, dashboard, workflow, form, role & permission management
  calendar    Calendar, event, and attendee management
  contact     Contacts operations
  docs        Document and content operations
  drive       File, comment, permission, and upload management
  event       Consume and manage real-time events
  im          Message and group chat management
  mail        Email, draft, folder, and contacts m
```

`orca` command groups (from `--help`):

```
orca

Usage: orca <command> [options]

Startup:
  open                      Launch Orca and wait for the runtime to be reachable
  serve                     Start a headless Orca runtime server
  status                    Show app/runtime/graph readiness

Diagnostics:
  diagnostics memory        Collect a memory snapshot for Orca and managed terminals

Agent Discovery:
  agent-context             Print the machine-readable command schema for agents

Accounts:
  account add               Add a managed Claude or Codex account on this Orca host
  account list              List managed Claude and Codex accounts on this Orca host

Skills:
  skills list               List version-matched skill guides bundled with this Orca CLI
  skills get                Print a version-matched skill guide as Markdown
  skills install            Install bundled Orca skills globally via the community skills CLI
  skills update             Update already-installed Orca skills via the community skills CLI

Environments:
  environment add           Save a remote Orca runtime from a pairing code
  environment list          List saved remote Orca runtimes
  environment show          Show one saved remote Orca runtime
  environment rm            Remove a saved remote Orca runtime

Environment Recipes:
  vm recipe doctor          Validate a per-workspace environment recipe

Automations:
  automations list          List scheduled Orca automations
  automations show          Show one Orca automation
  automa
```

### SSH host aliases (names only; addresses omitted)

- `github.com`, `macbook`, `ibex`, `ilogin`, `ibex-gpu`, `glogin`, `ibex-vscode`, `vscode`, `epfl-haas`, `git.gewu-lab.ai`, `ubuntu_box`, `arc-e2e-loopback`, `ubuntu_p2000_e2e`, `ubuntu_p2000_exec_e2e`, `local_p2000`, `ibex-cpu`

### Local listening services (port + process name only)

| Address | Port | Process |
|---|---|---|
| 0.0.0.0 | 7070 |  |
| 100.118.234.5 | 41497 |  |
| 0.0.0.0 | 1455 |  |
| 0.0.0.0 | 22 |  |
| 127.0.0.54 | 53 |  |
| 127.0.0.1 | 8436 | python |
| 127.0.0.1 | 8420 | python |
| 127.0.0.1 | 631 |  |
| 127.0.0.1 | 8088 |  |
| 127.0.0.1 | 42687 | python |
| 127.0.0.53%lo | 53 |  |
| 127.0.0.1 | 32933 | python |
| 127.0.0.1 | 36261 | python |
| 127.0.0.1 | 39411 | python |
| 127.0.0.1 | 39659 | python |
| [fd7a:115c:a1e0::fb01:ea9e] | 38400 |  |
| [::] | 7070 |  |
| [::] | 1455 |  |
| [::] | 22 |  |
| [::1] | 631 |  |

### Cloud / external API services (reachability + env presence)

| Service | Endpoint | Auth env | Env present | HTTP status |
|---|---|---|---|---|
| Bohrium LKM | https://open.bohrium.com/openapi/v2/lkm | BOHR_ACCESS_KEY | unset | 401 |
| Hugging Face Hub | https://huggingface.co | HF_TOKEN | unset | 200 |

### Documented external API services (secret-free)

#### Bohrium LKM

| Field | Value |
|---|---|
| Endpoint | https://open.bohrium.com/openapi/v2/lkm |
| Env vars | `BOHR_ACCESS_KEY` |
| Auth header | `Authorization: Bearer <key>` |
| Endpoints | `POST /search`, `POST /reasoning/search`, `POST /papers/graph`, `GET /claims/{id}/reasoning`, `POST /variables/batch`, `POST /feedback`, `POST /parse/task` |

Bohrium Large Knowledge Model (research-knowledge API). code==0 in the JSON body is app-level success (HTTP status alone is not sufficient). Official doc: dptech-corp/bohrium-skills zh/bohrium-lkm.

## Harbor interface surface

Top-level commands: `check`, `analyze`, `init`, `run`, `exec`, `publish`, `upload`, `add`, `download`, `remove`, `sync`, `view`, `adapter`, `task`, `dataset`, `job`, `hub`, `trial`, `cache`, `plugins`, `auth`, `agy`

| Interface | Harbor mapping |
|---|---|
| workspace | `--jobs-dir` / `-o` (default `jobs`) |
| task | `harbor task init/download/update/...` + `task.toml` / `dataset.toml` |
| timeout | `--timeout-multiplier`, `--agent-timeout-multiplier`, `--verifier-timeout-multiplier`, `--agent-setup-timeout-multiplier`, `--environment-build-timeout-multiplier` |
| result | `jobs/<job>/<trial>/` layout + reward + `verifier/test-stdout.txt` |
| trajectory | `harbor analyze` / `harbor view` (per-trial trajectory files) |
| auth | `harbor auth login` (GitHub OAuth) / `logout` / `status` / `key` / `org` |
| adapter | `harbor adapter init` / `review` |
| agent env | `--ak` / `--ae` with `${VAR}` template expansion; `--env-file`; `--allow-agent-host` |

Full `harbor run --help` (runtime observation):

```
Usage: harbor run [OPTIONS]                                                    
                                                                                
 Start a job. Alias for `harbor job start`.                                     
                                                                                
╭─ Options ────────────────────────────────────────────────────────────────────╮
│ --help  -h        Show this message and exit.                                │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ Config ─────────────────────────────────────────────────────────────────────╮
│ --config        -c      <str>  A job configuration path or GitHub URL in     │
│                                yaml or json format. Remote config URLs       │
│                                currently support GitHub only; Hugging Face   │
│                                and Harbor Hub native configs are planned.    │
│                                Should implement the schema of                │
│                                harbor.models.job.config:JobConfig. Allows    │
│                                for more granular control over the job        │
│                                configuration.                                │
│ --print-config                 Print the resolved JobConfig JSON and exit.   │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ Job Settings ───────────────────────────────────────────────────────────────╮
│ --job-name                       <str>               Name of the job         │
│                                                      (default: timestamp)    │
│ --jobs-dir               -o      <path>              Directory to store job  │
│                                                      results (default: jobs) │
│ --n-attempts             -k      <int>               Number of attempts per  │
│                                                      trial (default: 1)      │
│ --timeout-multiplier             <float>             Multiplier for task     │
│                                                      timeouts (default: 1.0) │
│ --agent-timeout-multip…          <float>             Multiplier for agent    │
│                                                      execution timeout       │
│                                                      (overrides              │
│                                                      --timeout-multiplier)   │
│ --verifier-timeout-mul…          <float>             Multiplier for verifier │
│                                                      timeout (overrides      │
│                                                      --timeout-multiplier)   │
│ --agent-setup-timeout-…          <float>             Multiplier for agent    │
│                                                      setup timeout           │
│                                                      (overrides              │
│                                                      --timeout-multiplier)   │
│ --environment-build-ti…          <float>             Multiplier for          │
│                                                      environment build       │
│                                                      timeout (overrides      │
│                                                      --timeout-multiplier)   │
│ --quiet,--silent         -q                          Suppress individual     │
│                                                      trial progress displays │
│ --debug                                              Enable debug logging    │
│ --n-concurrent           -n      <int>               Number of concurrent    │
│                                                      trials to run (default: │
│                                                      4)                      │
│ --n-concurrent-agents            <int range> [x>=1]  Per-agent cap on        │
│                                                      concurrent agent        │
│                                                      execution phases; must  │
│                                                      be no higher than       │
│                                                      --n-concurrent          │
│                                                      (default: unset)        │
│ --max-retries            -r      <int>               Maximum number of retry │
│                                                      attempts (default: 0)   │
│ --retry-include                  <str>               Exception types to      │
│                                                      retry on. If not        │
│                                                      specified, all          │
│                                                      exceptions except those │
│                                                      in --retry-exclude are  │
│                                                      retried (can be used    │
│                                                      multiple times)         │
│ --retry-exclude                  <str>               Exception types to NOT  │
│                                                      retry on (can be used   │
│                                                      multiple times)         │
│ --yes                    -y                          Auto-confirm prompts,   │
│                                                      including host          │
│                                                      environment access and  │
│                                                      sharing with            │
│                                                      organizations you are   │
│                                                      not a member of.        │
│ --env-file                       <path>              Path to a .env file to  │
│                                                      load into environment.  │
│ --install-only                                       Run agent setup/install │
│                                                      only, then exit. Skips  │
│                                                      the agent run and       │
│                                                      implies                 │
│                                                      --disable-verification. │
│                                                      Fast install            │
│                                                      compatibility check.    │
│ --artifact                       <str>               Environment path to     │
│                                                      download as an artifact │
│                                                      after the trial (can be │
│                                                      used multiple times)    │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ Agent ──────────────────────────────────────────────────────────────────────╮
│ --agent               -a      [aider|antigravity-cli  Agent to run, or a     │
│                               |claude-code|cline-cli  custom agent import    │
│                               |codex|computer-1|copi  path                   │
│                               lot-cli|cursor-cli|dee  (module.path:ClassNam… │
│                               rflow|devin|dspy-rlm|e  Also accepts an ACP    │
│                               ve|gemini-cli|goose|gr  registry shorthand     │
│                               ok-build|hermes|kimi-c  (e.g.                  │
│                               li|langgraph|mimo|mini  acp:opencode@1.3.9).   │
│                               -swe-agent|nemo-agent|                         │
│                               nop|openclaw|opencode|                         │
│                               openhands|openhands-sd                         │
│                               k|oracle|pi|qwen-coder                         │
│                               |rovodev-cli|swe-agent                         │
│                               |terminus|terminus-1|t                         │
│                               erminus-2|trae-agent|v                         │
│                               ibe|acp:<agent>]                               │
│ --model               -m      <str>                   Model name for the     │
│                                                       agent (can be used     │
│                                                       multiple times)        │
│ --ak,--agent-kwarg            <str>                   Additional agent kwarg │
│                                                       in the format          │
│                                                       'key=value'. You can   │
│                                                       view available kwargs  │
│                                                       by looking at the      │
│                                                       agent's `__init__`     │
│                                                       method. Can be set     │
│                                                       multiple times to set  │
│                                                       multiple kwargs.       │
│ --allow-agent-host            <str>                   Run-specific hostname  │
│                                                       or IP address/CIDR     │
│                                                       merged into the agent  │
│                                                       phase allowlist during │
│                                                       agent.run() only. Can  │
│                                                       be used multiple       │
│                                                       times.                 │
│ --resume-trajectory                                   For multi-step tasks,  │
│                                                       resume the agent's     │
│                                                       native session from    │
│                                                       the previous step      │
│                                                       instead of starting a  │
│                                                       fresh conversation on  │
│                                                       each step. Requires an │
│                                                       agent with native      │
│                                                       resume support.        │
│                                                       Per-step sessions:     │
│                                                       (fresh, resume,        │
│                                                       resume, ...) instead   │
│                                                       of the default (fresh, │
│                                                       fresh, fresh, ...).    │
│ --load-trajectory             <str>                   Path to a trajectory   │
│                                                       (ATIF) to load as the  │
│                                                       agent's session before │
│                                                       the first step. Not    │
│                                                       implemented yet;       │
│                                                       reserved interface.    │
│                                                       Per-step sessions:     │
│                                                       (load, fresh, fresh,   │
│                                                       ...); combined with    │
│                                                       --resume-trajectory:   │
│                                                       (load, resume, resume, │
│                                                       ...).                  │
│ --ae,--agen
```

## Documented provider wiring (secret-free)

### Environment variables present (names only — never values)

| Variable | Present |
|---|---|
| ANTHROPIC_API_KEY | unset |
| ANTHROPIC_AUTH_TOKEN | unset |
| ANTHROPIC_BASE_URL | unset |
| BOHR_ACCESS_KEY | unset |
| CODEX_AUTH_JSON_PATH | unset |
| DOCKER_HOST | unset |
| GITHUB_TOKEN | set |
| HF_TOKEN | unset |
| HUGGING_FACE_HUB_TOKEN | unset |
| OPENAI_API_KEY | unset |
| OPENAI_BASE_URL | unset |

### Anthropic-compatible Apex (claude-code)

| Field | Value |
|---|---|
| Endpoint | https://api.apexin.ai |
| Env vars | `ANTHROPIC_API_KEY`, `ANTHROPIC_BASE_URL` |
| Discovery | `opencode models <provider>`; `pi --list-models` |

Claude Code uses the Anthropic-compatible endpoint (no /v1 suffix). Model observation: claude-sonnet-5.

### OpenAI OAuth (codex / opencode)

| Field | Value |
|---|---|
| Env vars | `CODEX_AUTH_JSON_PATH` |
| Auth files (metadata) | `~/.codex/auth.json` |

Codex OpenAI OAuth is wired through CODEX_AUTH_JSON_PATH (a direct auth.json bind-mount is recorded as failed). OpenCode OAuth needs the custom provider module plus node_modules mounts.

### OpenAI-compatible Apex (codex / opencode)

| Field | Value |
|---|---|
| Endpoint | https://api.apexin.ai/v1 |
| Env vars | `OPENAI_API_KEY`, `OPENAI_BASE_URL` |
| Auth files (metadata) | `~/.codex/auth.json`, `~/.local/share/opencode/auth.json` |
| Discovery | `opencode models <provider>`; `pi --list-models` |

Codex and OpenCode share the OpenAI-compatible Apex endpoint. Model observations: openai/gpt-5.6-sol, openai/gpt-5.5.

## Safe preflight & smoke command shapes

Preflight (read-only, secret-free):

```
bash .agents/skills/development-machine-profile/scripts/probe-host.sh
```

Smoke (secret-safe: secrets via `${VAR}` expansion, never printed):

### Anthropic-compatible Apex (claude-code)

```
# Anthropic-compatible Apex (claude-code) — endpoint (metadata): https://api.apexin.ai
export ANTHROPIC_API_KEY="<value supplied at runtime; never printed>"
export ANTHROPIC_BASE_URL="https://api.apexin.ai"
harbor run -c <job-config>.json --yes --ae 'ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}' --ae 'ANTHROPIC_BASE_URL=${ANTHROPIC_BASE_URL}'
```

### OpenAI OAuth (codex / opencode)

```
# OpenAI OAuth (codex / opencode)
export CODEX_AUTH_JSON_PATH="<value supplied at runtime; never printed>"
harbor run -c <job-config>.json --yes --ae 'CODEX_AUTH_JSON_PATH=${CODEX_AUTH_JSON_PATH}'
```

### OpenAI-compatible Apex (codex / opencode)

```
# OpenAI-compatible Apex (codex / opencode) — endpoint (metadata): https://api.apexin.ai/v1
export OPENAI_API_KEY="<value supplied at runtime; never printed>"
export OPENAI_BASE_URL="https://api.apexin.ai/v1"
harbor run -c <job-config>.json --yes --ae 'OPENAI_API_KEY=${OPENAI_API_KEY}' --ae 'OPENAI_BASE_URL=${OPENAI_BASE_URL}'
```

## Evidence status

| Agent | Auth mode | Model | Status | Note |
|---|---|---|---|---|
| codex | OpenAI-compatible Apex | openai/gpt-5.6-sol | smoke-verified | hello-world reward 1.0 (prior runtime observation, 2026-09-03) |
| opencode | OpenAI-compatible Apex | openai/gpt-5.6-sol | smoke-verified | hello-world reward 1.0 (prior runtime observation, 2026-09-03) |
| claude-code | Anthropic-compatible Apex | claude-sonnet-5 | provider-reached-not-satisfied | provider calls succeeded but task-path mismatch (/hello.txt vs /app/hello.txt); not a full task success until rerun |

Status vocabulary: `smoke-verified` (task reward 1.0),
`provider-reached-not-satisfied` (provider calls succeeded, task not met),
`not-yet-verified`.

## Regeneration & maintenance

Facts are operator-confirmed metadata from prior runtime observations. Re-verify smoke evidence after Harbor/agent/model changes; live trial receipts are kept outside the repository. Endpoints, env-var names, and auth-file paths are metadata only — never key values.

Re-run the one-shot command at the top of this file after host, tool, or
provider changes. Non-probeable facts live in
`.agents/knowledge/development-machine-facts.md`; keep it secret-free and
date-stamped.
