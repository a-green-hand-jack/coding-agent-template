# Example Product

For the development coding agent only: maintain this example as a reusable product definition. Keep agent.yaml and runtime paths consistent. When copying this directory for a new product, update the manifest name and product identity. Do not put development instructions in product identity files.
